{
  "name": "lemonnekogh-ai",
  "type": "module",
  "private": true,
  "module": "index.ts",
  "scripts": {
    "build": "tstogd convert",
    "test": "vitest run --config test/vitest.config.ts",
    "test:build": "cd test && tstogd convert",
    "typecheck": "tsc -p tsconfig.json && tsc -p test/tsconfig.json && tsc -p test/tsconfig.node.json"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "@vidot/vitest": "workspace:*",
    "@vikeeper/vitest": "workspace:*",
    "typescript": "^5.9.3",
    "typescript-to-gdscript": "^0.1.3",
    "vitest": "4.1.10"
  }
}
