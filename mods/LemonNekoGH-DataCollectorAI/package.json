{
  "name": "lemonnekogh-ai",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "module": "index.ts",
  "scripts": {
    "build": "tstogd convert --tsconfig tsconfig.godot.json",
    "build:test-fixtures": "cd test && tstogd convert --tsconfig tsconfig.godot.json",
    "test": "pnpm --dir ../../packages/vikeeper run build && pnpm run build && pnpm run build:test-fixtures && vitest run --config test/vitest.config.ts",
    "typecheck": "tsc -p tsconfig.godot.json && tsc -p test/tsconfig.godot.json && tsc -p tsconfig.node.json"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@vidot/vitest": "workspace:*",
    "@vikeeper/vitest": "workspace:*",
    "typescript": "catalog:",
    "typescript-to-gdscript": "catalog:",
    "vitest": "catalog:"
  }
}
