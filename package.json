{
  "name": "airi-dome-keeper-root",
  "type": "module",
  "private": true,
  "packageManager": "pnpm@11.21.0",
  "scripts": {
    "alint": "alint .",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json && pnpm --recursive --if-present run typecheck",
    "build": "pnpm --recursive --parallel --if-present run build"
  },
  "devDependencies": {
    "@alint-js/cli": "catalog:",
    "@alint-js/plugin-js": "catalog:",
    "@antfu/eslint-config": "catalog:",
    "@types/node": "catalog:",
    "@vidot/vitest": "workspace:*",
    "eslint": "catalog:",
    "execa": "catalog:",
    "nanoid": "catalog:",
    "typescript": "catalog:",
    "typescript-to-gdscript": "catalog:",
    "vitest": "catalog:"
  }
}
