# AIRI Dome Keeper Plugin

## Documentation Rule
- When updating this file, always include references/links to the relevant docs or sources for new information.
- During discussion-only phases (no code changes yet), remind the user to update this file with the new decisions.
- Prefer automation across the workflow; automate any step that can be reliably automated, then use manual work only as a fallback.
- After discussions that change decisions/assumptions, update this file automatically (include references).

## AIRI Identity (Reference)
- "AIRI" refers to the open-source Project AIRI by `moeru-ai/airi` (web + desktop), which is a digital companion/virtual character platform. References: https://github.com/moeru-ai/airi , https://github.com/moeru-ai

## Architecture (Brief)
- **AIRI (top-level LLM):** Decides whether to start/stop gameplay. It interacts with the plugin at low frequency and may pause or terminate gameplay based on external context (e.g., time, messages).
- **Plugin Upper Agent (LLM):** Receives game-state summaries (via vision outputs) and produces mid-frequency, high-level tasks for gameplay (e.g., explore, mine, return to defend). It also reports concise status back to AIRI.
- **Plugin Lower Agent (controller):** Runs near-real-time control. It consumes the upper agent’s tasks and translates them into concrete keyboard/mouse actions based on the latest game state.
- **Vision Layer:** Uses object/UI detection to extract structured state from game frames. It feeds both agents, with lower latency to the lower agent and periodic summaries to the upper agent.
- **Experiment Orchestrator Agent (future outer loop):** Evaluates versioned gameplay, vision, teacher, and student runs; chooses a bounded improvement objective; produces and executes a reproducible training or controller-optimization plan; and compares each candidate with a fixed baseline before promotion. It is not part of real-time gameplay and must not rewrite its held-out evaluator during an experiment. References: internal project decision, https://mlflow.org/docs/latest/tracking/

## Goals
- Build an AIRI plugin that can autonomously play Dome Keeper using game frames as input and keyboard/mouse mappings as output.
- Support a two-layer agent structure: high-level LLM planning and low-level real-time control.

## Gameplay Mechanics (Reference)
- The Engineer mines by moving into a tile with the drill; tiles have health/hardness and take multiple hits to break. Reference: https://domekeeper.wiki.gg/wiki/Engineer
- Resources must be picked up and carried; carrying slows movement, and carry-strength upgrades reduce the slowdown (so you can carry more). Reference: https://domekeeper.wiki.gg/wiki/Engineer
- The Engineer has dedicated pickup and drop keybinds for carrying resources (pickup: Space by default, drop: Q by default). Reference: https://domekeeper.wiki.gg/wiki/Engineer
- Drill-strength upgrades make the drill stronger (mine harder rock). Reference: https://domekeeper.wiki.gg/wiki/Engineer

## Vision (YOLO) Plan (Brief)
- Use YOLO26n for MVP detection. Reference: https://docs.ultralytics.com/models/yolo26/
- Collect data via a gameplay capture tool that records frames while playing. Reference: https://docs.ultralytics.com/guides/data-collection-and-annotation/
- Start with a small class set (player, dome, enemy; optional ore). Reference: https://docs.ultralytics.com/datasets/detect/
- Fix training resolution (e.g., 640) and iterate after the end-to-end pipeline works. Reference: https://docs.ultralytics.com/modes/train/
- Initial detection classes: player (engineer), dome (laser), iron ore, cobalt ore, water ore, enemy (all monsters). Reference: internal project decision (no external doc).
- Use Ultralytics YOLO dataset YAML format (`data.yaml`) for train/val paths and class names. Reference: https://docs.ultralytics.com/datasets/detect/

## Immediate Milestone (Decision)
- Build a rule-based teacher collection demo for a single local Engineer using the Laser Dome in a manually started run. The demo covers mining, picking up and delivering nearby resources, returning to the dome, simple defense, and basic stuck recovery while the existing collector produces valid ore/enemy YOLO image-label pairs. References: internal project decision, https://domekeeper.wiki.gg/wiki/Engineer
- The superseded teacher state machine and its runtime wiring were intentionally removed before the revised seven-state controller was written. The replacement is now implemented and connected to the collector; do not reintroduce a compatibility shim or the superseded implementation. References: internal project decision, [rule_teacher.gd](mods/LemonNekoGH-YoloDataCollector/rule_teacher.gd), [mod_main.gd](mods/LemonNekoGH-YoloDataCollector/mod_main.gd)
- The user starts the run manually; automated loadout or menu navigation and restart after death are outside this milestone. Reference: internal project decision.
- Keep teacher actions realizable by the future lower action model: emit normal configured keyboard actions instead of directly changing keeper movement, weapon input fields, or Laser transforms. Before each mining trip, use horizontal input to align the Engineer with the dome's shaft x-coordinate before descending. On return, use open-tile A* only to reach the mine/cellar mouth, continue upward on the dome shaft until reaching the main Laser station's `Usable` height, and only then move horizontally to that exact interaction target; enter only when that main-station `Usable` is focused, never an optional cellar station. When neither an upgrade nor defense is required, transition to `NAVIGATE` while closing the station so the still-focused main computer cannot be entered again. After defense settles, remain inside the station and return to station task selection for the post-defense upgrade opportunity; close the station and transition to `NAVIGATE` only when that fresh selection finds no affordable upgrade or required defense. During defense, select a visible monster deterministically and use left/right/fire actions to close the signed error between the current Laser aim and the monster; do not randomly sweep or directly set the Laser angle. Restrict intentional aim targets to `Monster` instances registered in the supported team's active wave; never select nodes from projectile groups as pre-aim or fire targets, even when a weapon upgrade allows the Laser ray to collide with projectiles. Treat `Monster.Type.WORM_ROCK` as a projectile-like exception despite its `Monster` implementation, and exclude it from both pre-aim and fire targets because its high movement speed makes intentional Laser tracking unsuitable; leave it in the game's monster and wave lifecycle, never let it authorize firing, and tolerate only unavoidable one-physics-tick incidental collisions from cached ray and input timing. Prefer a visible, alive target that is currently damageable according to its runtime `canBeHit()` and `invulnerable` state. When no visible monster is currently damageable, still select a visible, alive monster for pre-aiming, as a human player would, but do not fire until that same runtime damage gate allows it; do not hard-code damage eligibility from the monster type or apparent animation phase. Run active Laser aiming on physics ticks to match the weapon's own update cadence while leaving the rest of the teacher state machine on its fixed real-time tick. Use signed angular error only to choose the steering direction until the Laser's enabled raycasts, evaluated in the weapon's own collision-priority order, report the exact selected monster as their first collider. Use the cached raycast result consumed by the weapon without forcing a same-frame refresh. Stop steering and fire only while that acquired target also passes the runtime damage gate; release fire immediately when acquisition is lost, and do not use a fixed angular dead zone or early-fire braking. Emit weapon controls only while the keeper is inside the station and `BattleInputProcessor` is the active input leaf, recover the battle input if a wave is still required, and wait for the game's `wavebattle` settlement before leaving the station. References: internal project decision, [rule_teacher.gd](mods/LemonNekoGH-YoloDataCollector/rule_teacher.gd), [Monster.gd](external/domekeeper-decompiled/5.0.5.19/content/monster/Monster.gd), [Monsters.gd](external/domekeeper-decompiled/5.0.5.19/content/monster/Monsters.gd), [Worm.gd](external/domekeeper-decompiled/5.0.5.19/content/monster/worm/Worm.gd), [Rock.gd](external/domekeeper-decompiled/5.0.5.19/content/monster/worm/Rock.gd), [MonsterSynchronizer.gd](external/domekeeper-decompiled/5.0.5.19/content/monster/MonsterSynchronizer.gd), [DirectProjectile.gd](external/domekeeper-decompiled/5.0.5.19/content/shared/projectiles/DirectProjectile.gd), [StationInputProcessor.gd](external/domekeeper-decompiled/5.0.5.19/content/dome/station/StationInputProcessor.gd), [Laser.gd](external/domekeeper-decompiled/5.0.5.19/content/weapons/laser/Laser.gd), https://docs.godotengine.org/en/4.3/classes/class_raycast2d.html, https://docs.godotengine.org/en/4.3/tutorials/physics/physics_introduction.html, https://docs.godotengine.org/en/4.3/tutorials/scripting/pausing_games.html, https://docs.godotengine.org/en/4.3/classes/class_inputmap.html, https://docs.godotengine.org/en/4.3/classes/class_input.html#class-input-method-parse-input-event, https://docs.godotengine.org/en/4.3/classes/class_node.html#class-node-method-get-meta
- While teacher collection is active, save and temporarily disable `Options.pauseWhenOutOfFocus` and `Options.useMouseDomeGameplay`, clear the game's `InputSystem.game_not_in_focus` gate, and keep `Input.MOUSE_MODE_VISIBLE` so the user can focus and operate another macOS app while the teacher continues to emit configured in-process keyboard events. Disabling mouse dome gameplay ensures incidental mouse motion after the game window receives focus cannot make `Laser.inputs()` replace the teacher's simultaneous keyboard steering and fire values with a stale mouse target. Restore both prior options when the teacher stops; do not persist the temporary overrides or use OS-level input injection. References: internal project decision, [BattleInputProcessor.gd](external/domekeeper-decompiled/5.0.5.19/content/dome/station/BattleInputProcessor.gd), [Laser.gd](external/domekeeper-decompiled/5.0.5.19/content/weapons/laser/Laser.gd), https://docs.godotengine.org/en/4.3/classes/class_input.html#class-input-method-parse-input-event, https://docs.godotengine.org/en/4.3/classes/class_input.html#enum-input-mousemode
- The future lower-model observation must expose the current Laser aim state, including exact selected-target ray acquisition, and each visible enemy's runtime damage eligibility and intentional-target eligibility in addition to its detection, including the `WORM_ROCK` projectile-like exclusion, because enemy position alone does not determine whether the next realizable action is left, right, pre-aim without firing, fire, or ignore. References: internal project decision, [Monster.gd](external/domekeeper-decompiled/5.0.5.19/content/monster/Monster.gd), [Rock.gd](external/domekeeper-decompiled/5.0.5.19/content/monster/worm/Rock.gd)
- Use the top-level teacher states `NAVIGATE`, `MINE`, `CARRY`, `RETURN`, `UPGRADE`, `DEFEND`, and `RECOVER`. `NAVIGATE` owns fishbone exploration and travel to saved task targets instead of representing shaft alignment, descent, branch return, and resume as separate top-level states. Reference: internal project decision.
- For the initial no-relic teacher, use a fixed reveal distance of one tile and calculate fishbone branch spacing as `1 + reveal_distance * 2`, producing branch rows `y`, `y + 3`, `y + 6`, and so on with two untouched rows between adjacent branches. Do not add dynamic reveal-radius handling yet. Enter `MINE` when the current view exposes a revealed ore vein, and never inspect unrevealed resource data. Before leaving `BRANCH` for `MINE`, save the keeper's current open branch tile; after the vein is cleared, retain that task across carry or defense interruptions, use open-tile A* in `NAVIGATE` to return to the saved tile, and only then resume the preserved branch direction. For a non-adjacent revealed ore target, compare the shortest open-tile A* approach ETA with the current direct cardinal tunneling ETA. Estimate tunneling from movement time plus only revealed intervening Tiles' remaining health, the runtime drill strength, hard-tile modifier, per-hit damage cap, drill buff, and hit cooldown; choose the open approach only when it is strictly faster, keep direct tunneling on a tie or when no open approach exists, and never inspect an unrevealed Tile to price a route. `MINE` clears the nearby revealed vein without picking up its drops, records the resulting loose resources as a pending cache site, and returns to `NAVIGATE`. Treat an adjacent, already revealed `border` Tile as a visually known fishbone branch endpoint: stay in `NAVIGATE`, return to the shaft through `ALIGN`, select the other side on the same row, and advance by the calculated branch spacing after completing that side; do not enter `RETURN` or probe the indestructible border through `RECOVER`. References: internal project decision, [rule_teacher.gd](mods/LemonNekoGH-YoloDataCollector/rule_teacher.gd), [Map.gd](external/domekeeper-decompiled/5.0.5.19/content/map/Map.gd), [Tile.gd](external/domekeeper-decompiled/5.0.5.19/content/map/tile/Tile.gd), [Keeper1.gd](external/domekeeper-decompiled/5.0.5.19/content/keeper/keeper1/Keeper1.gd), https://docs.godotengine.org/en/4.3/classes/class_astar2d.html#class-astar2d-method-get-point-path, https://docs.godotengine.org/en/4.3/classes/class_tilemap.html#class-tilemap-method-local-to-map, https://domekeeper.wiki.gg/wiki/Relic_Hunt#Bedrock
- When `DESCEND` sees an adjacent revealed `border` Tile below the current shaft, keep the top-level state as `NAVIGATE` and perform a bounded lateral bypass through normal movement and drill inputs. Search right first, reverse once if that side also reaches revealed border, and resume descent from the first column whose tile below is not confirmed border. If both horizontal directions terminate or the bypass cannot make directed progress, mark the current fishbone shaft exhausted instead of treating the terrain as a teacher failure: activate the shared persistent cache-cleanup task even below its normal two-full-load threshold, supersede any saved fishbone resume target, return to the dome, and use the same dynamically wave-safe repeated-trip policy while keeping teacher and data collection active for later upgrades and waves; never retry the same unreachable descent. Save the actual shaft intersection x-coordinate when the target fishbone row is reached so `ALIGN` returns to that entrance rather than the dome x-coordinate. A known border must never enter `RECOVER`; later trips may deterministically replay an open dogleg only while the shaft has not been exhausted. References: internal project decision, [rule_teacher.gd](mods/LemonNekoGH-YoloDataCollector/rule_teacher.gd), [Map.gd](external/domekeeper-decompiled/5.0.5.19/content/map/Map.gd), https://domekeeper.wiki.gg/wiki/Relic_Hunt#Bedrock, https://docs.godotengine.org/en/4.3/classes/class_astar2d.html#class-astar2d-method-get-point-path, https://docs.godotengine.org/en/4.3/classes/class_input.html#class-input-method-parse-input-event
- Maintain a deduplicated set of pending upgrade intents rather than a queue of concrete upgrade IDs. Seed `drill` as the initial pending intent when the teacher starts, then resolve pending intents in the fixed priority `combat > repair > drill > mobility`. Enqueue `combat` after a settled wave whose dome-health loss exceeds a configurable per-wave limit (initial default: 15% of maximum health), alternating its confirmed purchases between Laser attack strength first and dome maximum health. Enqueue `repair` whenever dome health is below 20%. Re-enqueue `drill` after its confirmed purchase only when one tile is subsequently observed taking at least a configurable maximum number of effective drill hits (initial default: five). At the start of each `CARRY` phase, calculate the maximum full load whose next resource would cross the accepted 55% speed-ratio floor and the maximum load that can return from the planned final pickup within the initial 15-second target. Enqueue `mobility` when safe capacity divided by full capacity is below the initial 75% threshold. Resolve the next supported movement-speed and carry-strength upgrades independently from current runtime state, apply their runtime `PropertyChange` semantics hypothetically, and prefer the arm that saves more return time over the same planned distance and final load; use speed as the deterministic tie. A newer `CARRY` plan may update this semantic arm until a concrete upgrade target is frozen for normal UI purchase. Each intent appears at most once. Treat only the exact TechTree prerequisites for supported health and repair targets as part of resolving those intents, and ignore all other technology branches. References: internal project decision, [rule_teacher.gd](mods/LemonNekoGH-YoloDataCollector/rule_teacher.gd), [Keeper1.gd](external/domekeeper-decompiled/5.0.5.19/content/keeper/keeper1/Keeper1.gd), [PropertyChange.gd](external/domekeeper-decompiled/5.0.5.19/systems/data/PropertyChange.gd), https://domekeeper.wiki.gg/wiki/Engineer, https://domekeeper.wiki.gg/wiki/Laser_Dome
- Do not store a requested level, concrete upgrade ID, or cost in the pending intent set or carry plan. At resource-reservation and purchase time, derive the next concrete upgrade from the keeper, weapon, and dome's current upgrade state and read its current runtime cost; this keeps repeatable upgrades and changing costs authoritative. Reserve stored and carried resources against resolvable intents in priority order, consuming every fully funded earlier cost before calculating the next deficit so the same mineral is never counted twice. Open the `CARRY` window dynamically when a load-capped preview plan's collection ETA plus loaded return ETA and the two-second station-entry margin reaches the remaining wave time. The margin reflects the observed 1.6–2.0 seconds from crossing the dome entrance to main-station task selection. Refresh this pathfinding-heavy preview at most once per second, while invalidating it immediately after cache, pickup, and upgrade changes; while actively mining, include the current vein location as a temporary cache site so already-loose drops can participate before the vein is fully cleared. On entering `CARRY`, diagnose mobility from that preview, then rebuild the bounded plan once so a newly requested mobility intent participates in the current trip's live resource reservation. Build each plan across all reachable drops associated with known cache sites: rank each next pickup first by the earliest unmet resource demand when one exists and then by its open-path travel time, use the current load for each outbound leg and the prospective load for the return leg, include the normal pickup delay, and admit it only while the complete plan can still reach the dome before the same station-entry margin. Treat this as a resource-type quota derived from a modeled greedy route rather than a strict route script: store only planned counts by runtime resource type, reselect a nearest valid matching drop while executing, and recheck the actual next leg against the hard wave budget. This keeps same-type focused drops interchangeable and avoids freezing stale concrete `Drop` references across the phase. The 15-second return target diagnoses mobility pressure but does not forbid an otherwise wave-safe pickup. Funding one intent does not trigger `RETURN`; continue against later intents, and treat minerals that do not yet reduce a pending deficit as lower-priority filler rather than ending the carry window. A pending upgrade intent or resource deficit is not required to enter `CARRY`; when the same safety limits allow it, the teacher may bring low-priority filler back to the dome. Leave behavior for overlapping drops of different resource types unchanged and undecided until recording evidence resolves it. Enter `RETURN` only when the next planned pickup would cross the accepted load-speed threshold, the plan's wave-safe return time is exhausted, no planned cached mineral remains, or a bounded pickup/path failure limit is reached. If a return finishes before the station-entry window, exit the station once. While the dynamic direct-return deadline is active, wait outside inside the dome without resuming descent or repeatedly entering the station; otherwise ordinary `NAVIGATE` work may resume. Re-enter when the remaining wave time reaches the station-entry margin or another confirmed station task requires it. Keep gameplay unpaused, and mine and carry only through normal configured movement, drill, pickup, and drop inputs. References: internal project decision, [rule_teacher.gd](mods/LemonNekoGH-YoloDataCollector/rule_teacher.gd), https://domekeeper.wiki.gg/wiki/Engineer, https://docs.godotengine.org/en/4.3/classes/class_astar2d.html#class-astar2d-method-get-point-path, https://docs.godotengine.org/en/4.3/classes/class_input.html#class-input-method-parse-input-event
- While mining, latch a pending cache-cleanup task when the count of known, reachable cached resource Drops reaches twice the current full-load count under the accepted 55% speed-ratio floor; reaching the threshold must not interrupt the current mining period. After the next settled `DEFEND` and its post-defense upgrade opportunity, close the station normally and start cleanup through the existing `CARRY` state only after `Keeper1InputProcessor` is active. During cleanup, execute as many dynamically wave-safe `CARRY`/`RETURN` trips as needed, including a final partial load. If a wave interrupts before all known reachable cached resources are cleared, retain the cleanup task through `RETURN`, `DEFEND`, and post-defense upgrade arbitration, then resume it instead of ordinary fishbone `NAVIGATE`. Clear the latch only when no known reachable cached resource remains; resources rejected by the existing bounded path or pickup failure policy must not block cleanup forever. Preserve and resume the interrupted fishbone work target after cleanup. Do not add an eighth top-level state, and keep the 15-second return target limited to mobility diagnosis rather than cleanup triggering or live pickup admission. References: internal project decision, [rule_teacher.gd](mods/LemonNekoGH-YoloDataCollector/rule_teacher.gd), [Keeper1.gd](external/domekeeper-decompiled/5.0.5.19/content/keeper/keeper1/Keeper1.gd), https://docs.godotengine.org/en/4.3/classes/class_astar2d.html#class-astar2d-method-get-point-path, https://domekeeper.wiki.gg/wiki/Engineer
- In the supported single-player run, provide independent upgrade-arbitration opportunities before and after defense. When the teacher reaches the dome and the highest-priority resolvable pending intent is affordable, enter `UPGRADE` before `DEFEND`, because the game's upgrade input pauses gameplay. Freeze that intent and its decision-time concrete target while driving the normal configured upgrade-menu and UI actions, clear the intent only after the matching purchase is confirmed, then resolve the remaining intents again from current runtime state. Close the menu through the normal input transition and then defend an active or imminent wave. After the game's `wavebattle` settlement returns control to the station, re-resolve pending intents and concrete targets from current runtime state instead of reusing a target frozen before defense, and enter `UPGRADE` again when the highest-priority target is affordable. This post-defense opportunity allows newly enqueued combat or repair intents to take priority over any older drill intent that remains pending and makes resources credited during defense available for purchases before the teacher leaves, rather than deferring an upgrade that has become affordable until a later return or defense cycle. An opportunity may confirm multiple sequential purchases through the existing re-resolution loop; it is not a requirement to buy when the highest-priority target is unaffordable. Keep TechTree frames excluded from the ore/enemy YOLO dataset while allowing the teacher to operate the popup only in `UPGRADE`. References: internal project decision, [rule_teacher.gd](mods/LemonNekoGH-YoloDataCollector/rule_teacher.gd), [GameWorld.gd](external/domekeeper-decompiled/5.0.5.19/game/GameWorld.gd), [Drop.gd](external/domekeeper-decompiled/5.0.5.19/content/drop/Drop.gd), https://docs.godotengine.org/en/4.3/tutorials/scripting/pausing_games.html, https://docs.godotengine.org/en/4.3/tutorials/inputs/inputevent.html#how-does-it-work
- Outside `RECOVER`, treat either one map-tile of directed movement or an effective drill hit as progress. After four seconds without either, enter `RECOVER` and try one-tile probes clockwise starting after the failed direction. During a recovery probe, an effective drill hit resets that probe's timeout but does not complete recovery; continue the same probe until the Engineer moves one directed map tile. Return to the interrupted state only after that movement, advance clockwise when a probe times out without movement or an effective drill hit, and fail closed after all four directions time out. Reference: internal project decision.
- Defer multiplayer, other keepers, non-Laser weapons, relic handling, dynamic reveal-radius behavior, and action-model dataset or training work until the revised teacher collection demo is proven. Reference: internal project decision.
- Prioritize making the current seven-state rule teacher reliably complete its supported mining, cache collection, return, upgrade, and defense loop before designing the future student action model's observation or task contract. Do not constrain current teacher improvements around an unconfirmed set of information available to the student; derive the student contract later from proven teacher behavior. References: internal project decision, [rule_teacher.gd](mods/LemonNekoGH-YoloDataCollector/rule_teacher.gd)

## Planned Milestones (TODO)

### Complete Relic Hunt

- After the revised teacher demo is proven, extend the supported manually started, single-player Engineer and Laser Dome run through the actual Relic Hunt completion condition: discover and activate revealed relic switches, excavate and open the relic chamber, pick up and carry the final relic to the dome through normal configured inputs, resolve the required final act, and confirm success only when the game reports `game.over == won`. Preserve the current revealed-information and realizable-action constraints; do not treat relic pickup or delivery alone as confirmed victory. References: internal project decision and target-version game investigation, https://domekeeper.wiki.gg/wiki/Relic_Hunt
- Treat gadget artifacts as a practical part of autonomous run completion rather than designing the completion policy around always ignoring or shredding them. Keep their transport and choice flow distinct from the final Relic Hunt relic: a gadget artifact opens a gadget-choice popup and installs a gadget whose controls, movement, resource, or combat effects may change later teacher behavior, while the final relic follows the relic-switch, relic-chamber, delivery, and final-act flow. Add gadget support incrementally through an explicit supported-gadget policy, and use shredding only as a fallback when no offered gadget has supported runtime semantics. References: internal project decision, [Chamber.gd](external/domekeeper-decompiled/5.0.5.19/content/map/chamber/Chamber.gd), [GadgetChoicePopup.gd](external/domekeeper-decompiled/5.0.5.19/stages/level/GadgetChoicePopup.gd), [LevelStage.gd](external/domekeeper-decompiled/5.0.5.19/stages/level/LevelStage.gd), [Relichunt.gd](external/domekeeper-decompiled/5.0.5.19/content/gamemode/relichunt/Relichunt.gd)
- Classify each gadget offer independently by its gameplay benefit (initially combat, survival/repair, movement, drilling, or carrying/logistics) and by its controller-integration impact. Benefit labels are descriptive and never grant support by themselves; select only exact, versioned gadget IDs on an explicit allowlist after each gadget's complete base runtime semantics have been validated against the current teacher. A gadget that requires no new key may still remain unsupported when it changes pathfinding, resource ownership or motion, usable focus, carried-object types, transient stats, or another existing planner assumption. Rank selectable offers by the matching current pending upgrade intent, use a deterministic ID tie order, and choose the exact `shredgadgettocobalt` fallback when no non-shred offer is supported. Selecting a gadget does not clear or fulfill the corresponding upgrade intent. Treat unknown gadget IDs as unsupported, and key the policy by stable runtime upgrade IDs rather than localized titles. References: internal project decision, [upgrades.yaml](external/domekeeper-decompiled/5.0.5.19/upgrades.yaml), [LevelStage.gd](external/domekeeper-decompiled/5.0.5.19/stages/level/LevelStage.gd), [Chainsaw.gd](external/domekeeper-decompiled/5.0.5.19/content/gadgets/chainsaw/Chainsaw.gd), [Lift.gd](external/domekeeper-decompiled/5.0.5.19/content/gadgets/lift/Lift.gd), [MushroomStation.gd](external/domekeeper-decompiled/5.0.5.19/content/gadgets/mushroomfarm/MushroomStation.gd)
- Keep the target-version gadget inventory and compatibility policy in a standalone, data-only GDScript catalog. List every canonical base gadget ID from the target `upgrades.yaml`, and place concise English comments beside each entry describing what it does and why the current teacher supports or rejects it. Store only selection facts that are not already authoritative in the game data, normalize team/player-prefixed runtime IDs before lookup, and fail closed for unknown IDs. Preload the catalog from the teacher so selection and the normal Godot parse check use the same policy source. References: internal project decision, [gadget_catalog.gd](mods/LemonNekoGH-YoloDataCollector/gadget_catalog.gd), [upgrades.yaml](external/domekeeper-decompiled/5.0.5.19/upgrades.yaml), [GameWorld.gd](external/domekeeper-decompiled/5.0.5.19/game/GameWorld.gd), [LevelStage.gd](external/domekeeper-decompiled/5.0.5.19/stages/level/LevelStage.gd)
- Handle `GadgetChoiceInputProcessor` as a mandatory cross-cutting modal instead of adding a top-level teacher state or reusing `UPGRADE`. Preserve the interrupted gameplay state and pending upgrade intents, wait for the popup's authoritative offer and animation readiness, freeze one target for that popup, and navigate to it only through configured `ui_*` actions. Rank supported offers by the first matching pending intent in the existing priority order, using the chosen speed/carry mobility arm, then fall back to a deterministic canonical-ID order among all supported offers, and finally choose the exact `shredgadgettocobalt` option when none are supported. Submit `ui_select` once, wait for the processor to close, require a selected non-shred ID to appear in `GameWorld.boughtUpgrades`, and never call popup, focus, or server-selection methods directly. References: internal project decision, [rule_teacher.gd](mods/LemonNekoGH-YoloDataCollector/rule_teacher.gd), [gadget_catalog.gd](mods/LemonNekoGH-YoloDataCollector/gadget_catalog.gd), [GadgetChoicePopup.gd](external/domekeeper-decompiled/5.0.5.19/stages/level/GadgetChoicePopup.gd), [GadgetChoiceInputProcessor.gd](external/domekeeper-decompiled/5.0.5.19/stages/level/GadgetChoiceInputProcessor.gd), [LevelStage.gd](external/domekeeper-decompiled/5.0.5.19/stages/level/LevelStage.gd)
- When a Gadget Chamber becomes revealed, immediately prioritize it over ordinary ore mining, resource collection, affordable upgrades, and merely imminent waves: excavate only its revealed remaining cover tiles, wait for it to open, activate its exact focused `Usable` through normal `ui_select`, and carry the spawned Gadget directly back to the dome as an exclusive load. Do not add a new top-level state or mix Gadget Drops into the resource quota planner; keep Chamber travel and work in `NAVIGATE`, freeze one current chamber task, and derive its phase from `Chamber.currentState`. Only an already-active monster wave and its settlement may interrupt excavation or activation. Require an empty load before deliberate activation; if the chamber is encountered while carrying resources, use the configured drop action to leave that cargo near the chamber, record dropped resources as a pending cache, and resume immediately instead of making an unloading round trip. Confirm both the chamber's authoritative `EMPTY` state and the deferred attachment of a newly spawned exact `Drop.type == CONST.GADGET` instance before returning; the broader `carryableType == "gadget"` category is not sufficient because it also covers unrelated artifacts and carryables. Keep the delivery task frozen until the exact Gadget popup completes, and operate only a popup whose `droptype == CONST.GADGET`. References: internal project decision, [rule_teacher.gd](mods/LemonNekoGH-YoloDataCollector/rule_teacher.gd), [Chamber.gd](external/domekeeper-decompiled/5.0.5.19/content/map/chamber/Chamber.gd), [GadgetChamber.gd](external/domekeeper-decompiled/5.0.5.19/content/map/chamber/gadget/GadgetChamber.gd), [GadgetDrop.tscn](external/domekeeper-decompiled/5.0.5.19/content/drop/gadget/GadgetDrop.tscn), [Keeper1InputProcessor.gd](external/domekeeper-decompiled/5.0.5.19/content/keeper/Keeper1InputProcessor.gd), [LevelStage.gd](external/domekeeper-decompiled/5.0.5.19/stages/level/LevelStage.gd)
- Before implementing relic control, define the minimum structured observations or additional vision classes needed for relic switches, the chamber, the carried relic, the dome drop point, and the terminal win/loss state. Keep those additions out of the current ore/enemy YOLO dataset until their annotation and evaluation contract is decided. Reference: internal project decision.

### Bounded Self-Evolution Experiment

- After repeatable end-to-end runs and the relevant student-training pipeline exist, build the Experiment Orchestrator Agent as an outer-loop evaluator and executor. Its input must cover gameplay elapsed time, win/loss counts and failure reasons; YOLO precision, recall, mAP50-95, and target-runtime latency; teacher task completion, state coverage, recovery/failure frequency, invalid-action rate, and path/action efficiency; and student held-out instruction-task success, teacher-action imitation metrics, rollout time, and win/loss rate. Treat instruction understanding as measured task performance rather than an LLM self-rating. References: internal project decision, https://docs.ultralytics.com/modes/val/, https://proceedings.mlr.press/v15/ross11a.html
- For each iteration, the agent must select a concrete objective, write a versioned plan containing its hypothesis, datasets, allowed optimization surface, compute/time/game-attempt budget, commands, acceptance thresholds, stop conditions, and rollback point, then execute only allowlisted collection, training, evaluation, and candidate teacher policy/state-machine optimization jobs within that budget. A behavior tree may be evaluated later as a candidate teacher architecture, but the current teacher remains a state machine. References: internal project decision, https://docs.ultralytics.com/guides/hyperparameter-tuning/, https://mise.jdx.dev/tasks/
- Keep datasets, model/controller revisions, prompts, configurations, seeds, hardware, timing/cost measurements, metrics, and artifacts attributable to an immutable experiment run. The planner may use training and validation results, but it must not alter or optimize against the held-out promotion evaluator during the same experiment. Candidate changes must beat the declared baseline gates without protected-metric regressions; merging, publishing, dependency-boundary changes, and replacing the baseline remain explicit approval gates. References: internal project decision, https://mlflow.org/docs/latest/tracking/, https://docs.ultralytics.com/modes/train/

## Modding Notes (Brief)
- Dome Keeper mods use the GDScript Mod Loader and require decompiling/importing the game into Godot, then placing mods under `res://mods-unpacked/Author-ModName` with `manifest.json` and `mod_main.gd`. Reference: https://github.com/DomeKeeperMods/Docs/wiki/Your-first-Mod
- The modding wiki recommends reviewing Mod Loader docs and proceeding to "Game Investigation" after the first mod setup. Reference: https://github.com/DomeKeeperMods/Docs/wiki/Your-first-Mod
- YOLO data collection mod targets Dome Keeper game version 5.0.5.19, Godot 4.3.1, and the official Dome Keeper Editor 5.1 custom build. References: internal project decision, https://github.com/DomeKeeperMods/Docs/wiki/Getting-Started , https://github.com/DomeKeeperMods/Docs/releases/tag/5.1 , https://docs.godotengine.org/en/4.3/about/introduction.html

## Mod Dev Workflow (Decision)
- Keep mod source in this repo under `mods/domekeeper/`, and link or copy it into the decompiled Godot project at `res://mods-unpacked/<Author>-<ModName>/` for testing. Reference: https://github.com/DomeKeeperMods/Docs/wiki/Your-first-Mod
- Do not use Mod Loader script extensions for base scripts that declare `class_name`; Godot 4 global classes are unsupported by that mechanism. Remove nonessential extensions instead of retaining a known parse failure, and use a script hook only when the behavior is required. Reference: https://wiki.godotmodding.com/guides/modding/script_extensions/
- For editor test runs, use a Mod Loader scene extension to disable `Game.devMode` while selecting the built-in custom-editor Level entry in memory. This keeps direct-to-level startup without development resources or disabled monster waves, does not persist editor configuration, and leaves retail startup unchanged. Reference: https://wiki.godotmodding.com/api/mod_loader_mod/#void-extend_scene-scene_vanilla_path-string-edit_callable-callable-static
- Run `mise run godot:check` after changing a Dome Keeper mod. The task starts the target project headlessly with an isolated temporary user-data directory, fails when a repository mod script reports a parse/load error or the YOLO collector does not enter the scene tree, and ignores known decompiled-project errors outside the repository mod boundary. Keep it in the aggregate `mise run check` task because Godot may exit successfully even after reporting a GDScript parse error. References: internal project decision, [check-domekeeper-mod.ts](scripts/check-domekeeper-mod.ts), https://docs.godotengine.org/en/4.3/tutorials/editor/command_line_tutorial.html
- Add a pause-menu toggle that starts/stops data capture; the collector owns this integration and injects the button when `SceneTree.node_added` reports a PauseMenu instance, avoiding a script extension of the game's preloaded PauseMenu. Each run creates `user://yolo_data/session_<timestamp>/` with `images/` and `labels/`, saving screenshots + YOLO labels at a fixed interval using object-tree queries. References: https://docs.godotengine.org/en/4.3/classes/class_scenetree.html#signal-scenetree-node-added , https://docs.godotengine.org/en/4.3/classes/class_basebutton.html#signal-basebutton-pressed , https://wiki.godotmodding.com/guides/modding/script_extensions/
- On session start, create a `data.yaml` alongside `images/` and `labels/` with YOLO dataset fields (`path`, `train`, `val`, `names`) for the captured classes. Reference: https://docs.ultralytics.com/datasets/detect/
- On session stop, open the capture folder in the OS file manager using Godot's `OS.shell_open`. Reference: https://docs.godotengine.org/en/stable/classes/class_os.html#class-os-method-shell-open
- Pause capture while the pause menu is visible by having the collector tag the dynamically observed menu panel with a group and check `CanvasItem.is_visible_in_tree()`. References: https://docs.godotengine.org/en/4.3/classes/class_scenetree.html#signal-scenetree-node-added , https://docs.godotengine.org/en/stable/classes/class_node.html#class-node-method-add-to-group , https://docs.godotengine.org/en/stable/classes/class_canvasitem.html#class-canvasitem-method-is-visible-in-tree
- Pause capture while the upgrade popup (TechTree) is visible by tagging `TechTreePopup` with the same group and checking visibility from the collector. References: https://docs.godotengine.org/en/stable/classes/class_node.html#class-node-method-add-to-group , https://docs.godotengine.org/en/stable/classes/class_canvasitem.html#class-canvasitem-method-is-visible-in-tree
- Capture frames are letterboxed to `640x640` with gray padding, and labels are transformed to match the padded image. Reference: https://docs.godotengine.org/en/4.2/classes/class_image.html
- Split each capture session into `train/val/test` by consecutive blocks of exactly 60 successfully saved image-label pairs, cycling blocks `4/1/1` as train, train, train, train, val, test. Compute the block from the accepted saved-image index after pause/overlay and target-balance rejection; a rejected capture advances neither the saved counter nor the block. Write paired files to `images/{split}` and `labels/{split}` and set `data.yaml` accordingly. References: internal project decision, https://docs.ultralytics.com/datasets/detect/
- Treat frames as having a target only when `enemy` or `ore_*` classes are present; allow one no-target frame only after 5 target frames have been captured. Reference: internal project decision (no external doc).
- Collect every active dome returned by the current game's `Level.getDomes()` API rather than the removed single-dome `Level.dome` property, so labels also cover multiplayer and versus runs. Verify game-facing APIs against the recovered target version during each version migration. References: internal project decision, https://github.com/DomeKeeperMods/Docs/wiki/Game-Investigation

## Decompiled Project Layout (Decision)
- Store decompiled Godot projects under `external/domekeeper-decompiled/<game-version>/` (version-isolated).
- Link the repo mod source into each decompiled project at `external/domekeeper-decompiled/<game-version>/mods-unpacked/<Author>-<ModName>/` for testing. Reference: https://github.com/DomeKeeperMods/Docs/wiki/Your-first-Mod

## Modding EULA & Repo Hygiene (Brief)
- Modding rules explicitly forbid distributing the decompiled project (including the `.pck`) and require that shared mod source contains only modded files, not original game files; they also recommend using a `.gitignore` to enforce this. Reference: https://github-wiki-see.page/m/DomeKeeperMods/Docs/wiki/Getting-Started
- Keep `tmp/` ignored because it may contain a machine-local link to the owned game PCK; never commit the link target or game contents. Reference: https://github-wiki-see.page/m/DomeKeeperMods/Docs/wiki/Getting-Started

## Decompile Script (Decision)
- It is acceptable to commit a local decompilation script as long as it does not include or distribute any game assets or decompiled outputs, and only operates on the user's local installation. Reference: https://github-wiki-see.page/m/DomeKeeperMods/Docs/wiki/Getting-Started
- Manage the required official Dome Keeper Editor with mise's GitHub backend. For the repository's Dome Keeper 5.0.5.19 target, pin editor release 5.1, a custom Godot 4.3.1 build containing the multiplayer modules used by the 5.0 update. Select exact editor assets and published SHA-256 digests for macOS arm64/x64, Linux x64, and Windows x64; unsupported architectures must fail instead of falling back to a mismatched artifact. Do not substitute a regular Godot or generic GodotSteam build, and keep the complete extracted bundle so its native Steam and multiplayer libraries remain beside the editor. References: https://github.com/DomeKeeperMods/Docs/wiki/Getting-Started , https://github.com/DomeKeeperMods/Docs/releases/tag/5.1 , https://mise.jdx.dev/dev-tools/backends/github.html
- The decompile script uses GDRETools CLI (`gdre_tools --headless --recover=... --output=...`) for one-step recovery, then links all repo mods under `mods/` into the decompiled `mods-unpacked/` directory. Reference: https://github.com/GDRETools/gdsdecomp
- Decompile script machine-local inputs are provided through `DOMEKEEPER_GAME_DIR`, `GDRETOOLS_BIN`, and optional `DOMEKEEPER_OUT_ROOT`, read via `import.meta.env`; mise provides the repository-owned `DOMEKEEPER_VERSION`. Godot tasks use the mise-managed editor and must not require a user-supplied `GODOT_BIN`. References: https://bun.com/reference/globals/ImportMeta , https://mise.jdx.dev/environments/
- For macOS stability, set the project rendering method to `forward_plus` in `project.godot` (rendering/renderer/rendering_method). Reference: https://docs.godotengine.org/en/stable/classes/class_projectsettings.html

## Repo Structure (Decision)
- Store each GDScript mod under `mods/<Author>-<ModName>/` (not under `crates/`). Reference: https://wiki.godotmodding.com/guides/modding/mod_structure/

## Dataset Versioning (Decision)
- For Hugging Face datasets, use `main` for ongoing work and use git tags for released dataset versions; create branches only for long-lived variants. References: https://huggingface.co/docs/huggingface_hub/guides/repository , https://huggingface.tw/docs/huggingface_hub/guides/repository

## Tooling (Decision)
- Use mise as the repository-level development-tool manager and task entry point. Pin tool versions in `mise.toml`; mise tasks delegate dependency-specific work to the owning package manager. Pin an LTS Node.js runtime because npm package executables such as ESLint and TypeScript use Node shebangs even when Bun launches their package scripts. Keep mise tool paths ahead of ambient package-manager paths so child-process shebangs resolve the pinned tools. References: https://mise.jdx.dev/dev-tools/ , https://mise.jdx.dev/tasks/ , https://mise.jdx.dev/configuration/settings.html#activate-aggressive , https://nodejs.org/en/about/previous-releases
- Enable mise lockfiles, commit `mise.lock`, and run `mise lock` whenever tool configuration changes so tool URLs and supported checksums remain reproducible. Commit `mise.toml` and `mise.lock` together. Reference: https://mise.jdx.dev/dev-tools/mise-lock.html
- Use Bun for JavaScript dependency installation and lockfile management. Keep `package.json` and `bun.lock`, and run TypeScript scripts with Bun. References: https://bun.sh/docs/pm/cli/install , https://bun.sh/docs/runtime/typescript

## Linting (Decision)
- Use ESLint with `@antfu/eslint-config` and the flat config (`eslint.config.mjs`). References: https://github.com/antfu/eslint-config , https://eslint.org/docs/latest/use/configure/configuration-files-new
- Treat `mise.toml` as mise-owned configuration: format and validate it with `mise fmt`, and exclude it from ESLint's conflicting TOML formatting rules. Reference: https://mise.jdx.dev/cli/fmt.html

## Exec Utilities (Decision)
- Prefer minimal, actively maintained process-exec libraries when available. For this repo, use `tinyexec` as the lightweight command runner. Reference: https://www.npmjs.com/package/tinyexec

## YOLO Python Dependency (Decision)
- Training/export will use the Ultralytics CLI (`yolo ...`), which is installed via the `ultralytics` Python package. Reference: https://docs.ultralytics.com/quickstart/
- Runtime inference can be Python-free by exporting models to formats like ONNX/NCNN/OpenVINO/TensorRT and running them in a non-Python runtime. Reference: https://docs.ultralytics.com/modes/export/

## Python Environment (Decision)
- Use mise to pin the Python and uv executables. Use the explicit `aqua:astral-sh/uv` backend so local legacy plugins cannot change uv resolution or weaken `mise.lock` metadata. Use uv as the sole owner of Python dependencies, `uv.lock`, and `.venv`; do not also create a mise-managed virtual environment or add Pixi for the same environment. Bind uv to mise's Python and disable uv-managed Python downloads so the interpreter has one owner. References: https://mise.jdx.dev/lang/python.html , https://mise.jdx.dev/dev-tools/backends/aqua.html , https://mise.jdx.dev/dev-tools/backend_architecture , https://docs.astral.sh/uv/guides/projects/ , https://docs.astral.sh/uv/reference/settings/#python-downloads
- Reconsider Pixi only if a confirmed requirement needs Conda-native system packages or independently solved accelerator environments; if adopted, Pixi must own Python and the complete environment rather than overlap with mise Python or uv. References: https://pixi.prefix.dev/latest/concepts/conda_pypi/ , https://pixi.prefix.dev/latest/python/pytorch/

## Inference Runtime (Decision)
- Run inference in the browser/Electron worker using `onnxruntime-web` with WebGPU where supported. References: https://onnxruntime.ai/docs/tutorials/web/ , https://onnxruntime.ai/docs/get-started/with-javascript/web.html
- WebGPU is available from Web Workers (via `WorkerNavigator.gpu`), enabling worker-based inference. Reference: https://developer.mozilla.org/en-US/docs/Web/API/WorkerNavigator/gpu

## Scripts Convention (Decision)
- `scripts/` should contain TypeScript scripts executable directly via Bun. Expose shared workflow entry points through mise tasks when repository-level orchestration is useful. References: https://bun.sh/docs/runtime/typescript , https://mise.jdx.dev/tasks/

## Monorepo Structure (Decision)
- `apps/airi-plugin/` for the AIRI plugin (TypeScript + Bun).
- `apps/vision/` for web inference entrypoints (Electron/Worker runtime).
- `apps/vision-playground/` for a WebGPU model playground and Hugging Face Spaces demo.
- `crates/capture/` for Rust performance-sensitive modules (capture/input/inference helpers).
- `mods/domekeeper/` for the GDScript mod (auto-labeling/metadata export).
- `packages/shared/` for shared types/protocols/schema.
- `packages/status-dashboard/` for the local Vue status observer.
- `scripts/` for Bun-executable TypeScript automation scripts.
- `data/` for datasets and labels, and `recordings/` for replay sessions (both ignored by git).

## Status Dashboard (Decision)
- Use the small Vue 3, TypeScript, and Vite package at `packages/status-dashboard/` to display and replay local Dome Keeper runs. Poll `GET /api/status` once per second in `Live` mode with no overlapping requests and keep the last valid snapshot visible when transport fails. The inline Vite `configureServer` plugin bridges only the live status file; replay files are selected manually and must not be discovered or served by Vite. Do not add demo/sample state, an HTTP server to the Godot mod, or a separate Node backend. References: internal project decision, https://vuejs.org/guide/typescript/overview.html , https://vite.dev/guide/api-plugin.html#configureserver
- Have the persistent rule teacher write its current live snapshot once per second to `airi-dome-keeper-status.json` in the operating system's temporary directory, resolving the same standard temporary-directory environment variables used by Node's `os.tmpdir()`. The existing Vite development process reads that file for `/api/status`; this live transport is separate from event-driven replay recording. References: internal project decision, https://docs.godotengine.org/en/4.3/classes/class_fileaccess.html , https://docs.godotengine.org/en/4.3/classes/class_os.html#class-os-method-get-environment , https://nodejs.org/api/fs.html#promises-api , https://nodejs.org/api/os.html#ostmpdir
- Use `@proj-airi/ui` with UnoCSS and its reset/style entrypoints so the observer shares AIRI's light, rounded, low-contrast visual language instead of introducing a separate game-HUD aesthetic. Use UnoCSS utilities and shortcuts for layout-specific status surfaces, keep raw CSS limited to document-level baseline rules, and use the library for shared primitives such as progress, callout, and button. References: internal project decision, https://www.npmjs.com/package/@proj-airi/ui , https://unocss.dev/integrations/vite
- Group each supported upgradeable runtime value with the level of its explicit bought-upgrade chain, using level zero for the base equipment and the numbered upgrade suffix after purchase. Keeper runtime information includes base and current effective movement speed with the `jetpackSpeed1..4` level, configured carry loss and current load slowdown with the `jetpackStrength1..4` level, drill strength with the `drill1..4` named level, and carried inventory. Dome runtime information includes current and maximum health with the `dome1health1..2` level, stored inventory, effective Laser attack strength with the `laserStrength1..4` level, and both normal and firing maximum Laser movement speed with the shared `laserMove1..3` level. Treat repeat purchases of `drill4` as changes to the runtime value while its named upgrade level remains four. Derive levels from explicit bought upgrade IDs, never from numeric values or visual variants, because modifiers and other upgrades may change the same runtime values. Do not duplicate Laser attack strength under Keeper data or expose raw Laser modifier fields alongside their effective products. Do not add teacher/controller labels, return-duration data, position/station fields, active-purchase state, or other fields outside the confirmed observer contract. Keep the `Upgrades` surface focused on pending intents plus the resolved next target and its current resource cost. References: internal project decision, [upgrades.yaml](external/domekeeper-decompiled/5.0.5.19/upgrades.yaml), [Keeper1.gd](external/domekeeper-decompiled/5.0.5.19/content/keeper/keeper1/Keeper1.gd), [Laser.gd](external/domekeeper-decompiled/5.0.5.19/content/weapons/laser/Laser.gd)
- Show the one-based current or upcoming wave number from the authoritative team `monsters.cycle + 1`, the wave time remaining, and active monsters grouped by runtime kind with the corresponding count and health data. Listen to the runtime cycle property so replay snapshots advance immediately after a settled wave; do not infer the number from recorded events. Do not add a separate battle-active or battle-settled callout because zero countdown plus a positive living count already communicates the active-wave condition. References: internal project decision, [Monsters.gd](external/domekeeper-decompiled/5.0.5.19/content/monster/Monsters.gd), [WaveMeter.gd](external/domekeeper-decompiled/5.0.5.19/content/hud/wavemeter/WaveMeter.gd)
- Keep the current-state summary data-led: do not add a decorative `DK` monogram tile or another invented avatar without game-state meaning. Reference: internal project decision.
- Keep exactly two dashboard modes. `Live` reads the current status snapshot. `Replay` requires the user to select both a replay JSON and its MP4, uses that movie as its only gameplay imagery, directly selects stored full snapshots, and provides play/pause, previous/next event, scrub/seek, and playback-speed controls while showing transition reasons. Replay must not discover a latest session, reconstruct state from patches, or display event screenshots. References: internal project decision, https://developer.mozilla.org/en-US/docs/Web/API/HTMLMediaElement/currentTime
- Create recorded sessions through one repository command that writes each session under `recordings/session_<filesystem-safe UTC ISO timestamp>_<UUID>/` in the repository root, then starts the target Godot project with Movie Maker enabled via `--write-movie` and an explicitly declared `--fixed-fps`. The timestamp makes folders human-readable and chronologically sortable, while the UUID preserves collision resistance; it is folder metadata only and must not participate in replay synchronization. Persist only the fixed FPS needed by replay synchronization; the user pairs the resulting JSON and MP4 by selecting both files. Do not depend on editor Movie Maker state, wall-clock speed, an external recorder, visual markers, or manual calibration. Movie Maker starts automatically after the splash screen and writes every rendered frame with fixed non-real-time simulation. If its built-in AVI output needs conversion for browser playback, automate that conversion while preserving the frame timeline. Shut the process down normally so the movie writer can finalize its output. References: internal project decision, https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString , https://docs.godotengine.org/en/4.3/tutorials/animation/creating_movies.html , https://docs.godotengine.org/en/4.3/tutorials/editor/command_line_tutorial.html , https://docs.godotengine.org/en/4.3/classes/class_moviewriter.html
- Make replay recording event-driven rather than periodic. At session start and on accepted `Data.listen` changes, resource pickup, monster spawn/death, confirmed upgrades, acquired-target firing-start observations, teacher state transitions, and terminal teacher failures, append an event containing a complete observer snapshot before teardown clears the recording state; never store RFC 6902 patches, viewport screenshots, PNG data, Base64 data, or duplicated video frames. Top-level teacher-state and `NAVIGATE` sub-state transitions must include their previous value, next value, and concrete trigger reason. Other tracked changes retain the complete current player, stat, dome, monster, and upgrade state needed to explain the run. References: internal project decision, [rule_teacher.gd](mods/LemonNekoGH-YoloDataCollector/rule_teacher.gd), https://docs.godotengine.org/en/4.3/classes/class_json.html , https://docs.godotengine.org/en/4.3/classes/class_fileaccess.html
- Use `Engine.get_process_frames()` as the authoritative Movie Maker frame clock. Record its movie-frame value for every full snapshot and persist the session's fixed FPS. Seek with `movie_frame / fixed_fps`, and do not use `Engine.get_frames_drawn()` because skipped draws can diverge from MovieWriter output. Keep `run_time_seconds` only as displayed unpaused game time. Do not synchronize by wall-clock timestamps, `Time.get_ticks_msec()`, event order, YOLO filenames, image recognition, or user input. Fail session startup if the movie/frame configuration is missing or inconsistent. References: internal project decision, https://docs.godotengine.org/en/4.3/classes/class_engine.html#class-engine-method-get-process-frames , https://docs.godotengine.org/en/4.3/classes/class_moviewriter.html , https://developer.mozilla.org/en-US/docs/Web/API/HTMLMediaElement/currentTime
- The live and replay observer contract exposes only: the player's current top-level state and current `NAVIGATE` sub-state when applicable; grouped Keeper movement, carry, and drill runtime values with their upgrade levels plus carried iron/cobalt/water; grouped Dome health and Laser attack/movement runtime values with their upgrade levels plus stored iron/cobalt/water; the one-based current or upcoming wave number, wave time remaining, and active monsters grouped by runtime kind with count and health; pending upgrade intents; and the resolved next upgrade with its current nonzero resource cost. Do not duplicate derived ratios, raw modifier inputs, monster totals, run time inside an event, or transition domains already identified by the event type. Because both JSON producers and consumers are owned by this repository, parse directly into the shared TypeScript contract instead of converting values to `unknown` and hand-writing pseudo-validation; treat JSON syntax and missing files as the actual boundary failures. References: internal project decision, [upgrades.yaml](external/domekeeper-decompiled/5.0.5.19/upgrades.yaml), [Keeper1.gd](external/domekeeper-decompiled/5.0.5.19/content/keeper/keeper1/Keeper1.gd), [Laser.gd](external/domekeeper-decompiled/5.0.5.19/content/weapons/laser/Laser.gd), [Monsters.gd](external/domekeeper-decompiled/5.0.5.19/content/monster/Monsters.gd), [WaveMeter.gd](external/domekeeper-decompiled/5.0.5.19/content/hud/wavemeter/WaveMeter.gd), [TechTreePopup.gd](external/domekeeper-decompiled/5.0.5.19/content/techtree/TechTreePopup.gd), [GameWorld.gd](external/domekeeper-decompiled/5.0.5.19/game/GameWorld.gd), [Const.gd](external/domekeeper-decompiled/5.0.5.19/systems/Const.gd)
- Keep Movie Maker/replay recording parallel to the YOLO dataset collector. Do not change YOLO classes, object-tree labels, `640x640` gray letterboxing, pause-menu/TechTree exclusion, target balancing, or image-label pairing for replay. Movie Maker may contain frames that YOLO rejects, and replay movie frames are not YOLO `capture_index` values. Do not reuse movie frames as labeled YOLO images. References: internal project decision, https://docs.godotengine.org/en/4.3/classes/class_image.html , https://docs.ultralytics.com/datasets/detect/

## Playground & Spaces (Decision)
- Use Hugging Face Spaces Static HTML SDK (`sdk: static`) for the WebGPU playground demo. References: https://huggingface.co/docs/hub/en/spaces-sdks-static , https://huggingface.tw/docs/hub/spaces-sdks-static
- Configure Spaces via the README YAML front-matter, using `app_build_command` and `app_file` when a build step is required (e.g., Vite). Reference: https://huggingface.co/docs/hub/en/spaces-config-reference
- Publish DomeKeeper YOLO v0 for the shared 2D game-playing playground using the existing model/dataset repository convention: the model card pins `playground.dataset_revision`, declares `playground.input_size`, and identifies `playground.output_format`. Export YOLO26 detection as the default end-to-end ONNX layout `(batch, max_det, 6)` and decode `[x1, y1, x2, y2, confidence, class_id]` without external NMS; retain an explicit `yolo-raw` path for existing YOLO11 models. References: internal project decision, https://docs.ultralytics.com/guides/end2end-detection , https://docs.ultralytics.com/modes/export
- DomeKeeper YOLO v0 is published publicly at `proj-airi/domekeeper-yolo-v0`, with its dataset at `proj-airi/domekeeper-yolo-dataset-v0`. The v0 model card pins dataset revision `f0fe78553d6adcd00b830724d11e3f71a955e0a8`; `examples/demo.jpg` is derived from `frame_000008.png`. Publish the verified ONNX artifact and evaluation material, but do not publish the unsanitized PyTorch checkpoint or raw training arguments because they embed machine-local paths. References: https://huggingface.co/proj-airi/domekeeper-yolo-v0 , https://huggingface.co/datasets/proj-airi/domekeeper-yolo-dataset-v0 , internal artifact review.
- The playground video MVP accepts local video files and uses the same inference-driven animation-frame loop as VNC: after one inference completes, the next animation frame copies the video element's current frame and starts another inference without waiting for a newly presented video frame. Keep at most one inference in flight, apply the same square letterbox preprocessing as image and VNC inputs, and stop processing when playback pauses, ends, the tab becomes inactive, or the component unmounts. Reference: internal project decision, https://developer.mozilla.org/en-US/docs/Web/API/Window/requestAnimationFrame

## Electron Integration (Decision)
- Use a Node native addon (Rust via `napi-rs`) to pass captured frames into Electron, instead of `electron-rs`.
