interface:
  display_name: Lint
  short_description: Run pinned ESLint fixes and TypeScript checks
  default_prompt: "Use $lint to run the repository's pinned lint and typecheck commands after code edits."
