[project]
name = "airi-dome-keeper-ml"
version = "0.0.0"
requires-python = ">=3.12,<3.13"
dependencies = [ "ultralytics>=8.4.102,<9", ]

[tool.uv]
package = false
python-downloads = "never"
python-preference = "only-system"
