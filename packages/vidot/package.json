{
  "name": "@vidot/vitest",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "exports": {
    ".": "./src/index.ts",
    "./setup": "./src/setup.ts"
  },
  "files": [
    "runtime/scripts",
    "src"
  ],
  "scripts": {
    "build": "cd runtime && bunx tstogd convert",
    "test": "vitest run --config ../../examples/basic-vidot/vitest.config.ts",
    "typecheck": "tsc -p tsconfig.json && tsc -p runtime/tsconfig.json"
  },
  "dependencies": {
    "tinyexec": "^1.0.2"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "typescript-to-gdscript": "^0.1.3",
    "vitest": "^4.1.10"
  }
}
