{
  "name": "@vidot/vitest",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "exports": {
    ".": {
      "godot": "./src/godot.ts",
      "node": "./src/node.ts"
    }
  },
  "files": [
    "runtime/runner.gd",
    "src"
  ],
  "scripts": {
    "build": "cd runtime && tstogd convert --godot-path \"$GODOT_BIN\" --project-root ../../../examples/basic-vidot",
    "prepack": "pnpm run build",
    "test": "pnpm run build && cd ../../examples/basic-vidot && tstogd convert --godot-path \"$GODOT_BIN\" --tsconfig tsconfig.json && vitest run --config vitest.config.ts",
    "typecheck": "tsc -p tsconfig.json && tsc -p runtime/tsconfig.json && tsc -p ../../examples/basic-vidot/tsconfig.json && tsc -p ../../examples/basic-vidot/tsconfig.node.json"
  },
  "dependencies": {
    "@vitest/runner": "catalog:",
    "execa": "catalog:",
    "typescript": "catalog:",
    "typescript-to-gdscript": "catalog:",
    "vitest": "catalog:"
  }
}
